% if ( $action ne 'browse' ) {
<a href="<%== include 'link/path', topath => 'browse' %>"><%= $t->{BROWSE} %></a>
% } else {
<%= $t->{BROWSE} %>
% }
<%= $sep %>
% if ( $action ne 'view' ) {
<a href="<%== include 'link/path', topath => 'view' %>"><%= $t->{VIEW} %></a>
% } else {
<%= $t->{VIEW} %>  
% }
<%= $sep %>
% if ( $action ne 'inspect' ) {
<a href="<%== include 'link/path', topath => 'info' %>"><%= $t->{INFO} %></a>
% } else {
<%= $t->{INSPECT} %>
% }