% if ( $runmode eq 'pair' ) {
%  if ( $pri eq 'folder' ) {
<%= $mapview->{$pri}->{$sec}->[1] %>
%  } elsif ( $pri eq 'date' ) {
<%= dtexpand ( $sec, $lang ) %>
%  } else {
<%= $sec %>
%   if ( $pri eq 'bcode' or $pri eq 'breed' ) {
<span class="lean"><%= $mapview->{pri}->{$sec}->[1] %></span>
%   } 
%  }
% } elsif ( $runmode eq 'search' ) {
<%= $t->{SEARCH_RESULT} %> <%= limit ( $what, 50 ) %>
% } else { # default to runmode 'all' 
<%= $t->{ALL} %> <%= $t->{PHOTOS} %>
% }