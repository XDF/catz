% if ( $runmode eq 'pair' ) {
%  if ( $pri eq 'folder' ) {
<%= $t->{ALBUM} %> <%= $mapview->{$pri}->{$sec}->[1] %>
%  } elsif ( $pri eq 'date' ) {
<%= $t->{DATE} %> <%= dtexpand ( $sec, $lang ) %>
%  } else {
<%= $t->{uc($pri)} %> <%= $sec %> 
%  }
% } elsif ( $runmode eq 'search' ) {
<%= $t->{SEARCH_RESULT} %> <%= limit ( $what, 100 ) %>
% } else { # default to runmode 'all' 
<%= $t->{ALL} %> <%= $t->{PHOTOS} %>
% }